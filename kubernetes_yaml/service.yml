apiVersion: v1
kind: Service
metadata:
  labels:
    app: health-ai
  name: health-ai-service
spec:
  ports:
  - port: 80 #change to 80
    protocol: TCP
    targetPort: 80 #change to 80
    nodePort: 30333
  selector:
    app: health-ai
  type: NodePort